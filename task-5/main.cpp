#include <iostream>
#include <cstring>
#include <string>
#include <cctype>
#include <algorithm>

using namespace std;

int main()
{


  string str;
  char b;
  int a = 0;



   getline(cin, str);

   while (str.length() < a)
   {
	   if (str == "." + 1)
	   {
		   toupper(b);
	   }

   }

   cout << str;
}